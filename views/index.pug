extends layout

block content
  .heading
    h1= title
    p Welcome to #{title}
    a(href='/new') Post New Message!

  each message in messages
    .msg
      h2= message.user 
      h4= message.text
      p added on #{message.added}

  